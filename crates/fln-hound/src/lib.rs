//! **fln-hound** — Bloodhound — in-toolchain hybrid search, premise retrieval, and proof repair over frankensearch; every candidate untrusted until the kernel accepts it (plan §16.1).
//!
//! Stub crate: charter only. Implementation arrives with its workstream beads;
//! the crate map and layering are governed by `WORKSPACE_GRAPH.txt` (bead fln-8mj).

#![forbid(unsafe_code)]
