//! **fln-server** — Lantern — the language server: the identical wire dialect from a single daemon with one shared immutable import heap and the machine session API (plan §14).
//!
//! Stub crate: charter only. Implementation arrives with its workstream beads;
//! the crate map and layering are governed by `WORKSPACE_GRAPH.txt` (bead fln-8mj).

#![forbid(unsafe_code)]
