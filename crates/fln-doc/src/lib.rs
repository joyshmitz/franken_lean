//! **fln-doc** — Folio — doc-gen4-compatible native HTML/PDF documentation with native TeX-math layout over franken_markdown, as an incremental build facet (plan §16.2).
//!
//! Stub crate: charter only. Implementation arrives with its workstream beads;
//! the crate map and layering are governed by `WORKSPACE_GRAPH.txt` (bead fln-8mj).

#![forbid(unsafe_code)]
