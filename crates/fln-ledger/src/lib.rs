//! **fln-ledger** — the Ledger — the build fabric: content-addressed declaration records over frankensqlite, the true-cone invalidation law, epochs, and cache federation over atp (plan §13).
//!
//! Stub crate: charter only. Implementation arrives with its workstream beads;
//! the crate map and layering are governed by `WORKSPACE_GRAPH.txt` (bead fln-8mj).

#![forbid(unsafe_code)]
