//! **fln-wasm** — the WASM Judge packaging — K1 + fln-checker + the sealed-capsule verifier compiled to a single WASM artifact: zero-install third-party verification (plan §16.4b).
//!
//! Stub crate: charter only. Implementation arrives with its workstream beads;
//! the crate map and layering are governed by `WORKSPACE_GRAPH.txt` (bead fln-8mj).

#![forbid(unsafe_code)]
