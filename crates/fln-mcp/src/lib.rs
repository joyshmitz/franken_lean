//! **fln-mcp** — Envoy — the MCP door over fastmcp_rust: budgeted, capability-scoped, receipt-carrying tools over O(1) proof-state snapshots (plan §16.3).
//!
//! Stub crate: charter only. Implementation arrives with its workstream beads;
//! the crate map and layering are governed by `WORKSPACE_GRAPH.txt` (bead fln-8mj).

#![forbid(unsafe_code)]
