//! **fln** — the embeddable library facade — parse/elaborate/check/query with a capability-first API: the embedder hands the engine a `Cx` (plan §17.2).
//!
//! Stub crate: charter only. Implementation arrives with its workstream beads;
//! the crate map and layering are governed by `WORKSPACE_GRAPH.txt` (bead fln-8mj).

#![forbid(unsafe_code)]
