//! **fln-anvil** — Anvil — the simp core on compiled discrimination-automata rewrite indexes, the e-graph saturation lane with kernel-checked extraction, and the `norm_num`/`omega` cores (plan §12).
//!
//! Stub crate: charter only. Implementation arrives with its workstream beads;
//! the crate map and layering are governed by `WORKSPACE_GRAPH.txt` (bead fln-8mj).

#![forbid(unsafe_code)]
