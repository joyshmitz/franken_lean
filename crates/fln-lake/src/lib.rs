//! **fln-lake** — the Lake-compatible build surface over the Ledger — `lakefile.lean` on Golem, the toml path, trace/manifest compatibility (plan §13.3).
//!
//! Stub crate: charter only. Implementation arrives with its workstream beads;
//! the crate map and layering are governed by `WORKSPACE_GRAPH.txt` (bead fln-8mj).

#![forbid(unsafe_code)]
