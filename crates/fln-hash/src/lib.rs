//! **fln-hash** — owned BLAKE3-class hashing, the domain-separation registry, and canonical serialization schemas for content addressing (plan §7, §21).
//!
//! Stub crate: charter only. Implementation arrives with its workstream beads;
//! the crate map and layering are governed by `WORKSPACE_GRAPH.txt` (bead fln-8mj).

#![forbid(unsafe_code)]
